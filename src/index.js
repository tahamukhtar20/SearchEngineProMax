const Crawler = require("./Crawler");
const BloomFilter = require("./BloomFilter");

// debugger;

// const bFilter = new BloomFilter();
const crawler = new Crawler();In case of running it in PySpark shell via pyspark executable, the shell autIn case of running it in PySpark shell via pyspark executable, the shell aut

crawler.start();
